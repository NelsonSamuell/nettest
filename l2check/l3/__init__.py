"""Layer 3 extension.

Adds the question the layer 2 side cannot answer: which layer 3 protections does
this network enforce, and what is reachable from where. Same four states, same
posture table, same rule that a check which learned nothing says UNTESTED.
"""
